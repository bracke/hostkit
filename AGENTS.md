# AGENTS.md

Agent guidance for this repository lives in [`CLAUDE.md`](./CLAUDE.md) — it is
tool-agnostic (build/test commands, the two rules that govern every change, conventions,
and the mandatory tri-platform CI). Read that file, and `src/hostkit.ads` for the full
rationale.
